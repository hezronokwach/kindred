name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.2.3'
      SERVERPOD_PASSWORD_database: 'KC9o_AIQhYZwZ17eC7qH2un0zT3uEugX'
      SERVERPOD_PASSWORD_redis: 'PpCYEsnL-J5kRQ3LgJ5gG2xuRor5Eb7o'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: kindred_butler_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: kindred_butler_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: kindred_butler_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: kindred_butler_server
        run: serverpod generate

      - name: Run tests
        working-directory: kindred_butler_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: kindred_butler_server
        run: docker compose down -v
